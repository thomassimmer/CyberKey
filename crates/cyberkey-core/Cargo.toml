[package]
name = "cyberkey-core"
version = "0.1.0"
edition = "2024"

[dependencies]
hmac     = { version = "0.12", default-features = false }
sha1     = { version = "0.10", default-features = false }
heapless = "0.8"
